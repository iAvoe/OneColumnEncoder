<Window x:Class="OneColumnEncoder.Views.WarnErrModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:OneColumnEncoder.Views"
        xmlns:comps="clr-namespace:OneColumnEncoder.Components"
        xmlns:vms="clr-namespace:OneColumnEncoder.ViewModels"
        d:DataContext="{d:DesignInstance Type=vms:WarnErrModalVM}"
        mc:Ignorable="d"
        d:DesignHeight="140"
        d:DesignWidth="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Background}">
    <!--
    | Title       |      |
    | P1: ...     |  I1  |
    | P2: ...     |      |
    I1: 3-row Icon (warning or error)
    P1: Message
    P2: Cancel, Confirm buttons
    -->
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0 5 0 0"
                   FontSize="16"
                   Text="{Binding P1Text}" />
        <comps:TwoButtonGroup Grid.Row="2"
                              Grid.Column="0"
                              DataContext="{Binding FinishWarnErrButtons}"
                              Button1Command="{Binding Cmd1}"
                              Button2Command="{Binding Cmd2}" />
        <!-- This might be an image icon instead -->
        <TextBlock Grid.Row="0"
                   Grid.RowSpan="3"
                   Grid.Column="1"
                   Margin="20 0 0 0"
                   Width="60"
                   Height="60"
                   FontSize="48"
                   Text="{Binding I1Text}" />
    </Grid>
</Window>
