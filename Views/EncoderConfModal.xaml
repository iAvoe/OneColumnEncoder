<ui:AdaptiveWindow x:Class="OneColumnEncoder.Views.EncoderConfModal"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:comps="clr-namespace:OneColumnEncoder.Components"
                   xmlns:ui="clr-namespace:OneColumnEncoder.UI"
                   xmlns:vms="clr-namespace:OneColumnEncoder.ViewModels"
                   d:DataContext="{d:DesignInstance Type=vms:EncoderConfVM}"
                   mc:Ignorable="d"
                   Title="{Binding WindowTitle}"
                   Width="1920"
                   SizeToContent="Height"
                   WindowStartupLocation="CenterOwner"
                   Background="{StaticResource Background}">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          IsEnabled="{Binding IsConfigUiEnabled}"
                          Padding="0 0 5 0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <comps:EncoderRateControlPanel />
                    <comps:EncoderCustomParamsPanel Margin="0 15 0 0" />
                </StackPanel>
            </ScrollViewer>

            <comps:TwoButtonGroup Grid.Row="1"
                                  ButtonHeight="30"
                                  Margin="0 5 5 0"
                                  DataContext="{Binding FinishButtons}"
                                  Button1Command="{Binding Cmd1}"
                                  Button2Command="{Binding Cmd2}" />
        </Grid>

        <Border Grid.Column="1"
                Width="1"
                Background="{StaticResource GlobalTertiary}" />

        <comps:EncoderPreviewPanel Grid.Column="2"
                                   DataContext="{Binding PreviewVM}" />
    </Grid>
</ui:AdaptiveWindow>
