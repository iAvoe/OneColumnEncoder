<UserControl x:Class="OneColumnEncoder.Components.EncoderProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30"
             d:DesignWidth="400">
    <UserControl.Resources>
        <Style x:Key="AvailableProgressBarStyle"
               TargetType="ProgressBar">
            <Setter Property="Height"
                    Value="20" />
            <Setter Property="Minimum"
                    Value="0" />
            <Setter Property="Maximum"
                    Value="100" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="2"
                                ClipToBounds="True">
                            <Border Margin="1"
                                    CornerRadius="2">
                                <Grid>
                                    <Border Background="{TemplateBinding Background}" />
                                    <Border x:Name="PART_Track" />
                                    <Border x:Name="PART_Indicator"
                                            HorizontalAlignment="Left"
                                            CornerRadius="2"
                                            Background="{TemplateBinding Foreground}" />
                                </Grid>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="UnavailableProgressBarStyle"
               TargetType="ProgressBar">
            <Setter Property="Height"
                    Value="20" />
            <Setter Property="Minimum"
                    Value="0" />
            <Setter Property="Maximum"
                    Value="100" />
            <Setter Property="Background"
                    Value="{StaticResource MemoryRangeEmpty}" />
            <Setter Property="Foreground"
                    Value="{StaticResource GlobalSecondary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="2"
                                ClipToBounds="True">
                            <Border Margin="1"
                                    CornerRadius="2"
                                    Opacity="1">
                                <Border.Background>
                                    <DrawingBrush Viewport="0,0,24,24"
                                                  ViewportUnits="Absolute"
                                                  TileMode="Tile"
                                                  Stretch="None">
                                        <DrawingBrush.Transform>
                                            <TransformGroup>
                                                <TranslateTransform x:Name="StripeOffset"
                                                                    X="0" />
                                                <RotateTransform Angle="35"
                                                                 CenterX="12"
                                                                 CenterY="12" />
                                            </TransformGroup>
                                        </DrawingBrush.Transform>
                                        <DrawingBrush.Drawing>
                                            <DrawingGroup>
                                                <GeometryDrawing Brush="#006C767D"
                                                                 Geometry="M 0,0 L 24,0 24,24 0,24 Z" />
                                                <GeometryDrawing Brush="#5A6C767D"
                                                                 Geometry="M 0,0 L 12,0 12,24 0,24 Z" />
                                            </DrawingGroup>
                                        </DrawingBrush.Drawing>
                                    </DrawingBrush>
                                </Border.Background>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate"
                                     Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard Name="IndeterminateAnim">
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetName="StripeOffset"
                                                              Storyboard.TargetProperty="X"
                                                              By="24"
                                                              Duration="0:0:8"
                                                              IsCumulative="True" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <StopStoryboard BeginStoryboardName="IndeterminateAnim" />
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ProgressVisibleWhenAvailableStyle"
               TargetType="Grid">
            <Setter Property="Visibility"
                    Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsProgressTrackingAvailable, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Value="True">
                    <Setter Property="Visibility"
                            Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ProgressVisibleWhenUnavailableStyle"
               TargetType="Grid">
            <Setter Property="Visibility"
                    Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsProgressTrackingAvailable, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Value="False">
                    <Setter Property="Visibility"
                            Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0"
                   Text="{Binding ProgressTitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource H3}"
                   VerticalAlignment="Center" />
        <!-- Mode 1: Progress available (nb_frames present) -->
        <Grid Grid.Column="1"
              Margin="7.5 0 10 0"
              Style="{StaticResource ProgressVisibleWhenAvailableStyle}">
            <ProgressBar Style="{StaticResource AvailableProgressBarStyle}"
                         Value="{Binding ProgressValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         Foreground="{StaticResource ProgressGreen}"
                         Background="{StaticResource MemoryRangeEmpty}" />
        </Grid>
        <!-- Mode 2: Progress unavailable -->
        <Grid Grid.Column="1"
              Margin="7.5 0 0 0"
              Style="{StaticResource ProgressVisibleWhenUnavailableStyle}">
            <ProgressBar Style="{StaticResource UnavailableProgressBarStyle}"
                         IsIndeterminate="{Binding IsEncodingActive, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Grid>
        <TextBlock Grid.Column="2"
                   Text="{Binding ProgressText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="13"
                   Foreground="{StaticResource GlobalSecondary}"
                   VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility"
                            Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsProgressTrackingAvailable, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     Value="False">
                            <Setter Property="Visibility"
                                    Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
