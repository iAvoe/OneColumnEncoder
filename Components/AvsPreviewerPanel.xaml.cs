namespace OneColumnEncoder.Components;

// The A/B viewer layout is format-independent; this named panel keeps the
// AviSynth dialog separate while reusing the established zoom interaction.
public class AvsPreviewerPanel : FrameServerPreviewerPanel { }
