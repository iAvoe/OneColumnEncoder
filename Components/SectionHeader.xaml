<UserControl x:Class="OneColumnEncoder.Components.SectionHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OneColumnEncoder.Components"
             xmlns:conv="clr-namespace:OneColumnEncoder.Converters"
             xmlns:viewmodels="clr-namespace:OneColumnEncoder.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="40"
             d:DesignWidth="450">
    <UserControl.Resources>
        <conv:StringToCharArray x:Key="StringToCharArray" />
    </UserControl.Resources>

    <Grid Grid.IsSharedSizeScope="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"
                              SharedSizeGroup="HeaderExtra" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"
                              SharedSizeGroup="HeaderExtra" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Grid.ColumnSpan="3"
                Height="1"
                VerticalAlignment="Center"
                SnapsToDevicePixels="True"
                Background="{StaticResource GlobalTertiary}" />

        <!-- Text part with letter spacing -->
        <ItemsControl Grid.Column="3"
                      VerticalAlignment="Center"
                      ItemsSource="{Binding Path=HeaderText,
                                    RelativeSource={RelativeSource AncestorType=local:SectionHeader},
                                    Converter={StaticResource StringToCharArray}}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               FontSize="13"
                               FontStretch="ExtraExpanded"
                               Foreground="{StaticResource GlobalTertiary}"
                               Margin="1 0 1 0" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Column="4"
                Height="1"
                VerticalAlignment="Center"
                SnapsToDevicePixels="True"
                Background="{StaticResource GlobalTertiary}" />

        <ContentPresenter Grid.Column="5"
                          VerticalAlignment="Center"
                          Focusable="False"
                          KeyboardNavigation.IsTabStop="False"
                          Content="{Binding HeaderExtra,
                                    RelativeSource={RelativeSource AncestorType=local:SectionHeader}}" />
    </Grid>
</UserControl>
