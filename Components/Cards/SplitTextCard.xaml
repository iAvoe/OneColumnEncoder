<UserControl x:Class="OneColumnEncoder.Components.Cards.SplitTextCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="900"
             Height="420">
    <UserControl.Resources>
        <ControlTemplate x:Key="SplitScrollViewerTemplate"
                         TargetType="ScrollViewer">
            <Grid Background="{TemplateBinding Background}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Margin="{TemplateBinding Padding}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        CanContentScroll="{TemplateBinding CanContentScroll}" />

                <ScrollBar x:Name="PART_HorizontalScrollBar"
                           Grid.Row="1"
                           Grid.Column="0"
                           Grid.ColumnSpan="2"
                           Orientation="Horizontal"
                           ViewportSize="{TemplateBinding ViewportWidth}"
                           Maximum="{TemplateBinding ScrollableWidth}"
                           Minimum="0"
                           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                           Value="{TemplateBinding HorizontalOffset}" />

                <ScrollBar x:Name="PART_VerticalScrollBar"
                           Grid.Row="0"
                           Grid.RowSpan="2"
                           Grid.Column="1"
                           Orientation="Vertical"
                           ViewportSize="{TemplateBinding ViewportHeight}"
                           Maximum="{TemplateBinding ScrollableHeight}"
                           Minimum="0"
                           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                           Value="{TemplateBinding VerticalOffset}" />

                <Rectangle x:Name="ScrollCorner"
                           Grid.Row="1"
                           Grid.Column="1"
                           Visibility="Collapsed" />
            </Grid>
        </ControlTemplate>

        <Style x:Key="SplitTextBox"
               TargetType="TextBox">
            <Setter Property="AcceptsReturn"
                    Value="True" />
            <Setter Property="AcceptsTab"
                    Value="True" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FontFamily"
                    Value="{DynamicResource CodeFont}" />
            <Setter Property="Foreground"
                    Value="{StaticResource GlobalPrimary}" />
            <Setter Property="HorizontalScrollBarVisibility"
                    Value="Auto" />
            <Setter Property="Padding"
                    Value="10" />
            <Setter Property="TextWrapping"
                    Value="NoWrap" />
            <Setter Property="VerticalScrollBarVisibility"
                    Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                SnapsToDevicePixels="True"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                          Template="{StaticResource SplitScrollViewerTemplate}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SplitRichTextBox"
               TargetType="RichTextBox">
            <Setter Property="AcceptsTab"
                    Value="True" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FontFamily"
                    Value="{DynamicResource CodeFont}" />
            <Setter Property="Foreground"
                    Value="{StaticResource GlobalPrimary}" />
            <Setter Property="HorizontalScrollBarVisibility"
                    Value="Auto" />
            <Setter Property="IsDocumentEnabled"
                    Value="False" />
            <Setter Property="IsReadOnly"
                    Value="True" />
            <Setter Property="Padding"
                    Value="5 10" />
            <Setter Property="VerticalScrollBarVisibility"
                    Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RichTextBox">
                        <Border x:Name="Bd"
                                SnapsToDevicePixels="True"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                          Template="{StaticResource SplitScrollViewerTemplate}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock.Style>
                <Style TargetType="TextBlock"
                       BasedOn="{StaticResource H3}">
                    <Style.Triggers>
                        <Trigger Property="Text"
                                 Value="">
                            <Setter Property="Visibility"
                                    Value="Collapsed" />
                            <Setter Property="Margin"
                                    Value="0" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Border Grid.Row="1"
                Margin="0 5 0 0"
                BorderBrush="{StaticResource GlobalPrimary}"
                BorderThickness="1.5"
                Background="{StaticResource Background}"
                ClipToBounds="True"
                CornerRadius="8"
                SnapsToDevicePixels="True">
            <Grid>
                <!-- Title, Left column, right column textboxes-->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"
                                      MinWidth="35" />
                    <ColumnDefinition Width="2" />
                    <ColumnDefinition Width="*"
                                      MinWidth="35" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <TextBox x:Name="LeftTextBox"
                             Text="{Binding LeftText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             FontSize="{Binding TextFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             TextChanged="OnTextChanged"
                             Padding="5 10">
                        <TextBox.Style>
                            <Style TargetType="TextBox"
                                   BasedOn="{StaticResource SplitTextBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRichTextMode, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <RichTextBox x:Name="LeftRichTextBox"
                                 FontSize="{Binding TextFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <RichTextBox.Style>
                            <Style TargetType="RichTextBox"
                                   BasedOn="{StaticResource SplitRichTextBox}">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRichTextMode, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RichTextBox.Style>
                    </RichTextBox>
                </Grid>

                <GridSplitter Grid.Column="1"
                              Width="2"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              Background="Transparent"
                              BorderBrush="{StaticResource GlobalSecondary}"
                              BorderThickness="1.5 0 0 0"
                              Cursor="SizeWE"
                              ResizeBehavior="PreviousAndNext"
                              ResizeDirection="Columns"
                              ShowsPreview="False" />

                <Grid Grid.Column="2">
                    <TextBox x:Name="RightTextBox"
                             Text="{Binding RightText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"
                             IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             FontSize="{Binding TextFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             TextChanged="OnTextChanged"
                             Padding="5 10">
                        <TextBox.Style>
                            <Style TargetType="TextBox"
                                   BasedOn="{StaticResource SplitTextBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRichTextMode, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <RichTextBox x:Name="RightRichTextBox"
                                 FontSize="{Binding TextFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <RichTextBox.Style>
                            <Style TargetType="RichTextBox"
                                   BasedOn="{StaticResource SplitRichTextBox}">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRichTextMode, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RichTextBox.Style>
                    </RichTextBox>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
