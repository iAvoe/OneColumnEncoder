<helpers:AdaptiveWindow x:Class="OneColumnEncoder.MainWindow"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:local="clr-namespace:OneColumnEncoder"
                        xmlns:helpers="clr-namespace:OneColumnEncoder.Helpers"
                        xmlns:views="clr-namespace:OneColumnEncoder.Views"
                        mc:Ignorable="d"
                        Title="1cenc (Commit 552)"
                        Width="920"
                        Height="1220"
                        WindowStartupLocation="CenterScreen">
    <!-- Horizontal scrollbar visibility prevents over stretching on ItemCards implicitly -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <!-- DataContext is inherited from parent (MainVM), so there is no binding needed here -->
        <views:MainUI />
    </ScrollViewer>
</helpers:AdaptiveWindow>