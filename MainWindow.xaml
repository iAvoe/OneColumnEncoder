<ui:AdaptiveWindow x:Class="OneColumnEncoder.MainWindow"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:local="clr-namespace:OneColumnEncoder"
                        xmlns:ui="clr-namespace:OneColumnEncoder.UI"
                        xmlns:views="clr-namespace:OneColumnEncoder.Views"
                        mc:Ignorable="d"
                        Title="1cenc"
                        Width="920"
                        Height="1220"
                        WindowStartupLocation="CenterScreen">
    <!-- If Commit number can be read, the Title will be replaced by this.xaml.cs -->
    <!-- Horizontal scrollbar visibility prevents over stretching on ItemCards implicitly -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <!-- DataContext is inherited from parent (MainVM), so there is no binding needed here -->
        <views:MainUI />
    </ScrollViewer>
</ui:AdaptiveWindow>
